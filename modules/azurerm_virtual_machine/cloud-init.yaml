#cloud-config
package_update: true
packages:
  - nginx
runcmd:
  - systemctl enable nginx
  - systemctl start nginx
  - echo '<h1>Nginx Installed from Terraform Cloud-Init</h1>' > /var/www/html/index.nginx-debian.html